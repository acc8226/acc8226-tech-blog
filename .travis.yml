language: node_js
node_js: stable

install: 
- npm install -g gulp
- npm install

script: 
- hexo g

after_script: 
- cd ./public 
- git init 
- git config user.name "Travis-CI"
- git config user.email "Travis-CI@feipig.fun" 
- git add . 
- git commit -m "Update docs with TRAVIS-CI.” 
- git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master

branches: 
  only: 
    - master 

env: 
  global: 
    - GH_REF: git@github.com:acc8226/acc8226.github.io.git # ssh
    